# dashboard 빌드/런타임 환경변수 예시
# 실제 값은 .env 로 복사해서 사용 (.env 는 git에 올리지 않음)

# API 백엔드 base URL (예: /api 또는 http://localhost:8000/api)
VITE_API_BASE_URL=/api

# true 면 MSW 목업 사용, false 면 실제 API 호출
VITE_USE_MOCK=false
